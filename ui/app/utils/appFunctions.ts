// import { functions } from '@dynatrace-sdk/app-environment'; // Removed: not exported in new SDK

/**
 * Utility module for calling app functions from the UI
 *
 * App functions are backend code that runs in the Dynatrace JavaScript runtime
 * This module provides a clean interface for the frontend to call these functions
 */

/**
 * Query data from Grail using DQL
 */
// export async function queryGrail(
//   query: string,
//   timeframe?: string
// ): Promise<unknown> {
//   return await functions.call('query-grail', { query, timeframe });
// }

/**
 * Get entities from Dynatrace
 */
// export async function getEntities(params?: {
//   entitySelector?: string;
//   from?: string;
//   to?: string;
//   pageSize?: number;
// }): Promise<unknown> {
//   return await functions.call('get-entities', params);
// }

/**
 * Get time-series metrics from Dynatrace
 */
// export async function getMetrics(params?: {
//   metricSelector?: string;
//   resolution?: string;
//   from?: string;
//   to?: string;
// }): Promise<unknown> {
//   return await functions.call('get-metrics', params);
// }
